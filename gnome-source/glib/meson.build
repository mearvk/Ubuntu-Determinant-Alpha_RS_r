project('glib', 'c',
  version : '2.89.4',
  meson_version : '>= 1.4.0',
  default_options : [
    'buildtype=debugoptimized',
    'warning_level=3',
    'c_std=gnu11',
  ]
)

# Upstream GNOME GLib Meson build definition.
# Imported as part of the project source-vendoring effort.
# Full upstream file is maintained at the GNOME mirror.
