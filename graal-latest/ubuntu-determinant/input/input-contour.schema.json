{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Ubuntu Determinant Input Contour Descriptor",
  "type": "object",
  "required": ["overall", "kind", "fash", "curl-color", "tone", "cause"],
  "properties": {
    "overall": { "type": "number", "minimum": 0, "maximum": 1 },
    "kind": { "type": "string", "minLength": 1 },
    "fash": { "type": "number", "minimum": 0, "maximum": 1 },
    "curl-color": { "type": "number", "minimum": 0, "maximum": 1 },
    "tone": { "type": "string", "minLength": 1 },
    "cause": { "type": "string", "minLength": 1 }
  },
  "x-ubuntu-determinant": {
    "concern-delimiter": 0.81,
    "comparison": "exact normalized numeric comparison",
    "meaning": "elevated input concern metadata; not a compiler semantic"
  }
}
