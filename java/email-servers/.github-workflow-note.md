# Build verification

The email-server project is Maven/Java 21. Run from this directory:

```sh
mvn -q test
mvn -q package
```

The repository connector does not provide a local Java/Maven runtime, so compilation must be verified by a Java 21 CI runner or local checkout.

**Author:** Max Rupplin - MEARVK LLC 2026
