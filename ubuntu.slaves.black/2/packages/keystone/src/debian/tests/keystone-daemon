#!/bin/bash
#------------------------
# Testing keystone-daemon
#------------------------
set -e

DAEMONS=('apache2')

service apache2 restart > /dev/null
sleep 5
if pidof -x apache2 > /dev/null; then
    echo "OK"
else
    echo "ERROR: ${daemon} IS NOT RUNNING"
    exit 1
fi
