--- gcc/debian/rules.defs
+++ gcc/debian/rules.defs
@@ -505,6 +505,8 @@ ifneq (,$(filter $(DEB_TARGET_ARCH), amd
   unstripped_exe = yes
 endif
 
+unstripped_exe = yes
+
 # common things ---------------
 # build common packages, where package names don't differ in different
 # gcc versions (fixincludes, ...)
