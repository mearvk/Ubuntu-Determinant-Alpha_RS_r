There is a simple instalation instruction.

1) unpack libdumbtts-0.x.y.tar.gz with command:
   tar -xzf libdumbtts-0.x.y.tar.gz
   (where 0.x.y is version of library)
   
2) cd libdumbtts-0.x.y/src

3) make

4) make install

5) ldconfig

You must be root to perform steps 4 and 5.

Bohdan R. Rau <ethanak@polip.com>
