# vim: set sts=4 expandtab:
IM_CONFIG_SHORT="$(gettext "do not set any IM from im-config and use desktop default")"

IM_CONFIG_LONG="$(gettext "This does not set any IM from im-config.
This is the automatic configuration choice if no required IM packages are installed, or if it is preferred to use desktop default.")"

# This always return true
package_auto () {
:
}

# This always return true
package_menu () {
:
}

