# One of the example scripts (not essential) requires assert.pl, so we
# add libperl4-corelibs-perl as a Recommended dependency only (cf. cdrdao)
libnet-dns-perl: script-uses-perl4-libs-without-dep (does not satisfy libperl4-corelibs-perl | perl (<< 5.12.3-7)) assert.pl [usr/share/doc/libnet-dns-perl/examples/contrib/check_zone:91]
