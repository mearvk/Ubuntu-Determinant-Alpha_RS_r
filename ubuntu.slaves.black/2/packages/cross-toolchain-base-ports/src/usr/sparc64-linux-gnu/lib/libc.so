/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf64-sparc)
GROUP ( /usr/sparc64-linux-gnu/lib/libc.so.6 /usr/sparc64-linux-gnu/lib/libc_nonshared.a  AS_NEEDED ( /usr/sparc64-linux-gnu/lib64/ld-linux.so.2 ) )
