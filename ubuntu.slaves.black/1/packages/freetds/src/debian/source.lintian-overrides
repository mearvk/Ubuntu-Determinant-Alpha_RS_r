# upstream idiosyncracy to use an ELF binary as test data for a sql query;
# the source really doesn't matter because it's genuinely being used as data,
# never as software.
freetds source: source-is-missing src/dblib/unittests/data.bin
