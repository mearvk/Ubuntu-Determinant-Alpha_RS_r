# Although gradle-docs-*.jar is codeless, Gradle treats it as a submodule and
# fails to launch without it.
codeless-jar usr/share/java/gradle-docs-*.jar
codeless-jar usr/share/java/gradle-runtime-api-info-*.jar