From: Balint Reczey <balint.reczey@canonical.com>
Date: Wed, 20 Jan 2021 18:09:01 +0100
Subject: Use Thread.is_alive() for Python 3.9 compatibility

---
 agent/hibagent | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/agent/hibagent b/agent/hibagent
index 435703b..99b5929 100755
--- a/agent/hibagent
+++ b/agent/hibagent
@@ -255,7 +255,7 @@ class BackgroundInitializerRunner(object):
     def check_finished(self):
         if self.thread is not None:
             self.thread.join(timeout=0)
-            if self.thread.isAlive():
+            if self.thread.is_alive():
                 return False
             self.thread = None
 
