From: Balint Reczey <balint.reczey@canonical.com>
Date: Fri, 1 Mar 2019 22:33:57 +0100
Subject: Update grub configuration when it needs an update

Also log update steps accurately
---
 agent/hibinit-agent | 24 +++++++++++++-----------
 1 file changed, 13 insertions(+), 11 deletions(-)

diff --git a/agent/hibinit-agent b/agent/hibinit-agent
index 9c8e295..45f04e4 100755
--- a/agent/hibinit-agent
+++ b/agent/hibinit-agent
@@ -93,21 +93,23 @@ def get_partuuid(device):
 
 
 def patch_grub_config(swap_device, offset, grub2_dir):
-    log("Updating GRUB to use the device %s with offset %d for resume" % (swap_device, offset))
-    lines = []
-
-    # Do GRUB2 update as well
+    """Update GRUB2 config when needed"""
     if grub2_dir and os.path.exists(grub2_dir):
         offset_file = os.path.join(grub2_dir, '99-set-swap.cfg')
-        if not os.path.exists(offset_file):
-            if swap_device.startswith("/dev"):
-                swap_device = "PARTUUID=%s" % get_partuuid(swap_device)
-                log("Updating GRUB referencing root device as %s" % (swap_device))
+        if swap_device.startswith("/dev"):
+            swap_device = "PARTUUID=%s" % get_partuuid(swap_device)
+        grub_snippet = (
+            'GRUB_CMDLINE_LINUX_DEFAULT="$GRUB_CMDLINE_LINUX_DEFAULT'
+            ' no_console_suspend=1 resume_offset=%d resume=%s"\n'
+            % (offset, swap_device))
+        if ((not os.path.exists(offset_file)
+             or open(offset_file).read() not in (grub_snippet))):
+            log("Updating GRUB to use the device %s with offset %d for resume"
+                % (swap_device, offset))
             with open(offset_file, 'w') as fl:
-                fl.write('GRUB_CMDLINE_LINUX_DEFAULT="$GRUB_CMDLINE_LINUX_DEFAULT no_console_suspend=1 '
-                         'resume_offset=%d resume=%s"\n' % (offset, swap_device))
+                fl.write(grub_snippet)
             check_call('/usr/sbin/update-grub2')
-    log("GRUB configuration is updated")
+            log("GRUB configuration is updated")
 
 
 def update_kernel_swap_offset(swapon, swapoff, filename, grub_update):
