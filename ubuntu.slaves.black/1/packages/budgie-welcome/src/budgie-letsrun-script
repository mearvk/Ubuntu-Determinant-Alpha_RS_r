#!/bin/bash

REMIX=$(which budgie-remix-desktop)
if [ $? -ne 0 ]; then
    apt purge -y lightdm-gtk-greeter
fi
  
apt purge -y budgie-welcome
apt install -y ubuntu-budgie-desktop
apt autoremove -y
snap install ubuntu-budgie-welcome --classic
reboot --
