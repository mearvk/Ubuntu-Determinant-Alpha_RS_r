# our -dev package contains a symlink to the soname, i.e. .so -> .so.2
# the .so.2 -> .so.2.whatever symlink is provided by the libfbclient2 package
# so we are guaranteed not to ship broken symlinks
firebird3.0 source: weak-library-dev-dependency
