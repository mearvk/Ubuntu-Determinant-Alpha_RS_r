diff --git a/debian/rules.defs b/debian/rules.defs
index 04165fb..ff1958b 100644
--- a/debian/rules.defs
+++ b/debian/rules.defs
@@ -873,6 +873,11 @@ ifeq ($(single_package),yes)
     ada_no_snap := yes
   endif
 endif
+ifeq ($(DEB_CROSS),yes)
+  ifneq (,$(filter $(DEB_TARGET_ARCH),alpha))
+    ada_no_cross := yes
+  endif
+endif
 
 ifeq ($(with_dev),yes)
   ifneq ($(separate_lang),yes)
