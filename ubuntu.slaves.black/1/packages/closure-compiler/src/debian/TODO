- The ant build fails if svn is not installed. TODO: Fix upstream

- The ant build tries to fetch an svn revision to build.

- what about the js code in externs/ ? is it needed? It's bundled as a zip in
   the jar. And the js code in contrib/externs?
