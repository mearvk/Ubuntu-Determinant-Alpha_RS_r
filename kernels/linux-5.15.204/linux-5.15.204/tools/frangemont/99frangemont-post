// frangemont-postmail-ps: APT Post-Invoke Hook (Stage 4: Post-Install, MANDATORY)
// Scans all files installed by the package AFTER installation completes.
// This stage ALWAYS runs regardless of prior scan results.
// Installed to: /etc/apt/apt.conf.d/99frangemont-post
DPkg::Post-Invoke { "/usr/local/lib/frangemont-apt-hook post || true"; };
