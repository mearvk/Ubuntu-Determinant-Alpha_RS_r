# USB — Company Recognition

Recognition key: `(USB, Company)`.

## Example associations

- `(USB, Intel)` — USB host-controller ecosystem; verify controller generation.
- `(USB, AMD)` — chipset/host-controller ecosystem; verify exact platform.
- `(USB, Renesas)` — USB controller ecosystem; verify exact controller.
- `(USB, ASMedia)` — USB controller ecosystem; verify exact device.
- `(USB, VIA)` — historical USB controller ecosystem; verify exact device/generation.

## Grade

**3/5 provisional:** class-level recognition is clear; exact VID/PID, controller, driver, and source provenance are required for higher grades.

## Time derivative

Record USB generation, class, controller, device firmware, and driver lineage independently.

**Project attention:** Max Rupplin — MEARVK LLC — 2026.