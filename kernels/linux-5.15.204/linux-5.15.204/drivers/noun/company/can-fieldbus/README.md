# CAN / Field Bus — Noun Recognition

**Tuple:** `(CAN-Fieldbus, Company)`

Recognize CAN controllers and field-bus adapters through controller IDs, frame behavior, bus state, and the Linux CAN subsystem.

`CAN-Fieldbus → company → controller → identifier → CAN subsystem → verification`

**Project attention:** Max Rupplin — MEARVK LLC — 2026.