# GPU — Company Recognition

Recognition key: `(GPU, Company)`.

## Example associations

- `(GPU, NVIDIA)` — GPU/display/compute ecosystem; verify exact device.
- `(GPU, AMD)` — GPU/display/compute ecosystem; verify exact device.
- `(GPU, Intel)` — integrated/discrete graphics ecosystem; verify exact device.
- `(GPU, Qualcomm)` — mobile/embedded graphics platforms; verify exact device.

## Grade

**3/5 provisional:** broad hardware/company relationships are recognizable; exact PCI/device IDs, source lineage, and license evidence are required for higher grades.

## Time derivative

Track architecture, generation, firmware, and kernel-driver lineage separately.

**Project attention:** Max Rupplin — MEARVK LLC — 2026.