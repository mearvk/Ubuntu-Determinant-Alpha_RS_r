# PLACEHOLDER — ICC Legal Standing 3

The exact original **ICC Legal Standing 3** `.docx` is pending lossless upload.

This placeholder exists so the folder structure is in place. It will be replaced
by `ICC/ICC-Legal-Standing-3.docx` (the exact original bytes) once the file is
provided as base64, or by `ICC/ICC-Legal-Standing-3.md` if the readable text is
provided instead. See `ICC/README.md` for the procedure.

No content has been fabricated. The corrupted paste of the binary was not
committed, because it would not have preserved the document.
