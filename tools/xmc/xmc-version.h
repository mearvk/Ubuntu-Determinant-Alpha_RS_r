#ifndef XMC_VERSION_H
#define XMC_VERSION_H

/* Keep this value synchronized with the XMC core until the core consumes
 * this header directly. The version is intentionally 1.0.0 for the current
 * released source tree. */
#define XMC_VERSION_MAJOR 1
#define XMC_VERSION_MINOR 0
#define XMC_VERSION "1.0.0"

#endif
