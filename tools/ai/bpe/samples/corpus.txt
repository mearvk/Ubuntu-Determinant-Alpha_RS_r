The quicksilver algorithm disperses tokens across a manifold of directions.
Attention multiplies queries against keys, then softmaxes the resulting scores.
A tokenizer maps text into integers; the embedding matrix maps integers into vectors.
Byte-pair encoding greedily merges the most frequent adjacent symbol pair.
Rotary embeddings encode position by rotating query and key vectors.
Superposition lets a network store more concepts than it has neurons.
Quantization compresses weights while preserving their relative geometry.
Grammar-constrained decoding forbids any token that would violate a production rule.
The decider samples one token from the distribution the network produces.
Vocabulary growth shortens sequences but does not raise the reasoning ceiling.
Nucleus sampling keeps the smallest set of tokens covering the target probability mass.
Temperature sharpens or flattens the distribution before a token is drawn.
