# Ubuntu White GNOME Extensions

Optional GNOME Shell extensions belong here when theme/CSS/settings are insufficient.

Extensions must be narrowly scoped, documented, and kept independent of the upstream GNOME source where practical. Do not use an extension to duplicate functionality already supplied by GNOME or Mutter.