#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$SCRIPT_DIR"

if [ -x "./.gradle-dist/bin/gradle" ]; then
  exec "./.gradle-dist/bin/gradle" -b desktop-synthesizer.gradle run "$@"
fi

exec gradle -b desktop-synthesizer.gradle run "$@"
