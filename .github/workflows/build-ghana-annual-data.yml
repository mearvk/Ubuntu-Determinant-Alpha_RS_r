name: Build Ghana annual statistical archive

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'tools/ghana/build_ghana_annual_data.py'
      - '.github/workflows/build-ghana-annual-data.yml'
  schedule:
    - cron: '29 10 1 * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build annual records
        run: python3 tools/ghana/build_ghana_annual_data.py
      - name: Commit generated records
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add national/ghana
          if git diff --cached --quiet; then echo "no changes"; exit 0; fi
          git commit -m "Populate Ghana annual statistical data 1965-2026"
          for i in 1 2 3 4 5 6 7 8; do
            git pull --rebase --autostash origin main && git push origin HEAD:main && exit 0
            echo "push attempt $i failed; retrying"; sleep $((RANDOM % 15 + 5))
          done
          echo "push failed after retries"; exit 1
