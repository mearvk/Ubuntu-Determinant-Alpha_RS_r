name: Build Moldova annual statistical archive

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'tools/moldova/build_moldova_annual_data.py'
      - '.github/workflows/build-moldova-annual-data.yml'
  schedule:
    - cron: '2 15 1 * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build annual records
        run: python3 tools/moldova/build_moldova_annual_data.py
      - name: Commit generated records
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add national/moldova
          if git diff --cached --quiet; then echo "no changes"; exit 0; fi
          git commit -m "Populate Moldova annual statistical data 1965-2026"
          for i in 1 2 3 4 5 6 7 8; do
            git pull --rebase --autostash origin main && git push origin HEAD:main && exit 0
            echo "push attempt $i failed; retrying"; sleep $((RANDOM % 15 + 5))
          done
          echo "push failed after retries"; exit 1
