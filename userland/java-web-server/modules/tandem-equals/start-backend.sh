#!/bin/bash
# TandemEquals™ — Start Backend (port 49223)
cd "$(dirname "$0")/.." || exit 1
echo "[*] Starting TandemEquals backend..."
# Started via NWE Main.java — this script is for standalone testing
echo "    TandemEquals is started by NWE Main.java (port 49223)"
echo "    To test standalone: java -cp /path/to/classes source.TandemEqualsServer"
